import { Link } from '@inertiajs/react';

export default function LineSlideBar({ active = false, className = '', children, ...props }) {
    return (
        /* Linea Horizontal Divisoria, divide contenido ------*/
        /* separa conteido superior de inferior hr */
        <hr className="w-full" />
    );
}
